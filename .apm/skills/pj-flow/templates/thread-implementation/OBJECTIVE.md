# OBJECTIVE — Implementation系 Thread

> 実装系Thread。purposeは `implementation` / `pivot`（方針転換に伴う再実装）/ `fix`（レビュー後の追加修正）など。

## 参照Thread(s)

本Threadが成果を受け継ぐThread:

- `<PROJECTS_ROOT>/<slug>/threads/<ref-thread>/OUTPUT.md`  # 多くの場合は直近のconcrete/reconcrete
- （必要なら複数）

## 前提の扱い

- 参照ThreadのOpen Questions 解決状況: （未解決のものは明示。未解決のまま進める判断ならその理由も）
- 仕様変更インパクト: （小/中/大）
  - **大** の場合、本来は `reconcrete` Threadを先に立てるべき。それを省略している理由を書く

## このThreadの対象範囲

- repo: （server / client / app / admin 等）
- 対象ファイル群（推定）:
  -

## 実装タスク

- [ ] （タスク 1）
- [ ] （タスク 2）
- [ ] テスト追加 / 更新
- [ ] ローカル動作確認
- [ ] PR対象ブランチへ push

## Done定義

- [ ] 上記タスクが全て完了
- [ ] 対象repoで静的チェック / テストが pass
- [ ] ローカル動作確認の証跡（スクショ / コマンド出力）を本フォルダに保存
- [ ] OUTPUT.md を書き切った

## Non-Goal

- （このThreadで意図的にやらないこと。次Threadに送るものを明示）
